joy_environment() {
  pkgin up
  pkgin in ImageMagick-6.6.6.5
}

joy_mongo() {
  if [[ `config_get stack` != 'node' ]]; then return 0; fi
}

joy_redis() {
  if [[ `config_get stack` != 'node' ]]; then return 0; fi
}